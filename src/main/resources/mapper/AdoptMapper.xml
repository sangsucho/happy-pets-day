<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.happypetsday.mapper.AdoptMapper">

    <select id="getCenterByCenterName" resultType="adoptDto">
        SELECT CENTER_NUMBER, CENTER_NAME, CENTER_ADDRESS, CENTER_CALL_NUMBER
        FROM TBL_CENTER
        WHERE CENTER_NAME = #{centerName}

    </select>


</mapper>